{% set git = salt['grains.filter_by']({
    'default': {
      'install_from_source': 'False'
    },
    'Debian': {
        'git': 'git',
    },
    'Gentoo': {
        'git': 'dev-vcs/git',
    },
}, merge=salt['pillar.get']('git:lookup')) %}
